<!DOCTYPE html>
<html>
<head>
  <title>Papereviews</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
    <!--[if lt IE 9]>
    <script src="http://html5shim.googlecode.com/svn/trunk/html5.js"></script>
    <![endif]-->
</head>
<body>
    <header class="navbar navbar-fixed-top">
      <div>
        <div class="container">
          <%= link_to "paper reviews", root_path, id: "logo" %>
          <nav>
	    <table width="60%"><tr><td style="width:30%; text-align:right;"><br/><%= link_to "View Reviews",reviews_path %></td>
			<td style="width:30%; text-align:center"><br/><%= link_to "Create Review",new_review_path %></td>
		<td>
			<% if user_signed_in? %>
		        <div style="float:right">Welcome <%= current_user.email %> <%= link_to "Logout", destroy_user_session_path, :method => :delete%></div>
		      <% end %>
		</td></tr></table>
          </nav>
        </div>
      </div>

    </header>
    <div class="container">
	<% if notice != nil %>
          <p class="notice"><%= notice %></p>
	<%end%>
	<% if alert != nil %>	
	  <p class="alert"><%= alert %></p>
        <%end%>
	<%= yield %>
	<%= render 'layouts/footer' %>
<%= debug(params) if Rails.env.development? %>
    </div>
</body>
</html>
